{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "npm:unpublishSafe",
    "helpers:pinGitHubActionDigests"
  ],
  "minimumReleaseAge": "3 days",
  "internalChecksFilter": "strict",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 5am on monday"]
  },
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  },
  "osvVulnerabilityAlerts": true,
  "dependencyDashboardOSVVulnerabilitySummary": "unresolved",
  "packageRules": [
    {
      "description": "Roll up routine GitHub Actions bumps into a single weekly PR (security PRs are unaffected)",
      "matchManagers": ["github-actions"],
      "groupName": "github actions",
      "schedule": ["before 5am on monday"]
    },
    {
      "description": "Minimum Release Age cannot be enforced for lockFileMaintenance on non-npm ecosystems — the package manager controls version resolution",
      "matchDatasources": ["!npm"],
      "matchUpdateTypes": ["lockFileMaintenance"],
      "minimumReleaseAge": null,
      "prBodyNotes": [
        "⚠️ Renovate's lockFileMaintenance does not validate Minimum Release Age — the package manager performs the version resolution. Review fresh transitive updates manually."
      ]
    },
    {
      "description": "Minimum Release Age cannot be enforced for package replacements on non-npm ecosystems",
      "matchDatasources": ["!npm"],
      "matchUpdateTypes": ["replacement"],
      "minimumReleaseAge": null,
      "prBodyNotes": [
        "⚠️ Renovate's replacement flow does not currently wire in release-age metadata (renovatebot/renovate#39400). Manually validate the Minimum Release Age for the replacement package."
      ]
    },
    {
      "description": "Minimum Release Age cannot be enforced for pin updates on non-npm ecosystems",
      "matchDatasources": ["!npm"],
      "matchUpdateTypes": ["pin"],
      "minimumReleaseAge": null,
      "prBodyNotes": [
        "⚠️ Renovate's pin flow does not currently wire in release-age metadata (renovatebot/renovate#40288). Manually validate the Minimum Release Age for the pinned version."
      ]
    }
  ]
}
